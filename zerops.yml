zerops:
  - setup: nodejs

    build:
      base: nodejs@24
      buildCommands:
        - cd backend && npm install

      deployFiles:
        - ./

    run:
      base: nodejs@24

      start: cd backend && npm start

      ports:
        - port: 3000
          httpSupport: true