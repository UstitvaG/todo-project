zerops:
  - setup: nodejs
    build:
      base: nodejs@24
      buildCommands:
        - npm install
      deployFiles:
        - ./
    run:
      base: nodejs@24
      start: npm start
      ports:
        - port: 3000
          httpSupport: true