zerops:
  - setup: nodejs

    build:
      base: nodejs@24

      buildCommands:
        - cd backend && npm install

      deployFiles:
        - backend

    run:
      base: nodejs@24

      start: node backend/server.js

      ports:
        - port: 3000
          httpSupport: true