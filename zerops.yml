zerops:
  - setup: nodejs

    build:
      base: nodejs@24
      workingDir: backend

      buildCommands:
        - npm install

      deployFiles:
        - ./

    run:
      base: nodejs@24
      workingDir: backend

      start: npm start

      ports:
        - port: 3000
          httpSupport: true
